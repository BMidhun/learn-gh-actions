name: 04-Using-Actions
on:
    push
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: "Go to the code"
              run: ls
            - name: "Printing Folders"
              run: ls